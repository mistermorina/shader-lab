# Architecture Notes

## Routing Struktur
No routing directory detected from safe metadata.

## UI Komponenten
React-family app detected. Query Graphify for component trees and shared UI primitives.

## State Management
- No state-management dependency detected from package metadata.

## API oder Backend Bereiche
No backend/API directory detected from safe metadata.

## Datenmodelle
No data model directory detected from safe metadata.

## Auth falls vorhanden
No auth metadata detected from safe manifest/directory scan. Use Graphify queries before opening raw files.

## Build System
- `dev`
- `build`

## Testing Setup
- No test scripts detected in package metadata.

## Wichtige Abhaengigkeiten
- `@types/react`
- `@types/react-dom`
- `@vitejs/plugin-react`
- `react`
- `react-dom`
- `vite`
